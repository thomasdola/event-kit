import _ from 'lodash';
import { createSelector } from 'reselect';

const getOrders = ({ orders }) => orders;

// export const getOrderMetaSelector = createSelector(
//     [getOrders], orders => 
// );