import expect from 'expect';
import React from 'react';
import { mount, shallow, render } from 'enzyme';

import Cart from './cart';

describe('Cart Component', () => {
    
    // const cartWrapper = render(<Cart/>);

    // it('should contain cart item list component', () => {
    //     expect(cartWrapper.find("div.Cart__Item__List").length).toBe(1);
    // });

    // it('should contain cart total component', () => {
    //     expect(cartWrapper.find("div.Cart__Total").length).toBe(1);
    // });
});
